# 備份功能完整使用教學

## 📚 目錄

1. [備份功能簡介](#備份功能簡介)
2. [如何建立備份](#如何建立備份)
3. [如何查看備份](#如何查看備份)
4. [如何復原備份](#如何復原備份)
5. [如何管理備份](#如何管理備份)
6. [實戰範例](#實戰範例)
7. [常見問題](#常見問題)

---

## 備份功能簡介

### 🎯 備份功能做什麼？

當你使用 AI 生成的命令可能會：
- 刪除檔案（`rm`）
- 移動檔案（`mv`）
- 修改檔案（`sed`, `>`）

**備份功能會自動：**
1. 在執行前備份這些檔案
2. 給每個備份一個唯一 ID
3. 保存到 `~/.aichat_backups/`
4. 讓你隨時可以復原

### 🔑 核心概念

```
執行命令前 → 自動備份 → 執行命令 → 出錯？→ 一鍵復原
```

---

## 如何建立備份

### 方式 1：自動備份（推薦）⭐

使用 `-e` (Execute 模式) 時，系統會**自動建立備份**。

#### 步驟：

**1. 準備測試環境**
```bash
# 建立測試目錄
mkdir -p /tmp/backup-test
cd /tmp/backup-test

# 建立測試檔案
echo "這是重要資料" > important.txt
echo "這是測試檔案" > test.txt
```

**2. 執行 AI 命令**
```bash
# 設定別名
alias ai='/Users/morrisliao/Desktop/git-repo/aichat/aichat/target/release/aichat'

# 執行可能會刪除檔案的命令
ai -e "刪除 test.txt"
```

**3. AI 生成命令並顯示選項**
```
rm test.txt
[p]review | [e]xecute | [r]evise | [t]utor | [c]opy | [q]uit: _
```

**4. 按 'e' 執行**
```
✓ Backup created: f8e961a3-b4c2-4d8f-9e7a-1234567890ab
Executing: rm test.txt
✓ Command executed successfully
```

🎉 **備份已建立！** 備份 ID 是：`f8e961a3-b4c2-4d8f-9e7a-1234567890ab`

---

### 自動備份的時機

備份會在這些情況**自動觸發**：

| 命令類型 | 範例 | 是否備份 |
|---------|------|---------|
| 刪除檔案 | `rm file.txt` | ✅ 是 |
| 移動檔案 | `mv file.txt backup/` | ✅ 是 |
| 覆寫檔案 | `echo "new" > file.txt` | ✅ 是 |
| 修改檔案 | `sed -i 's/old/new/' file.txt` | ✅ 是 |
| 讀取檔案 | `cat file.txt` | ❌ 否 |
| 列出檔案 | `ls -la` | ❌ 否 |

---

## 如何查看備份

### 使用 `.backup list` 命令

```bash
# 1. 啟動 REPL
ai

# 2. 列出所有備份
> .backup list
```

### 輸出範例

```
📦 Backup History
================================================================================

[1] ID: f8e961a3-b4c2-4d8f-9e7a-1234567890ab
    Time: 2025-12-05T15:30:00+08:00
    Command: rm test.txt
    Files backed up: 1
      - /tmp/backup-test/test.txt

[2] ID: a1b2c3d4-e5f6-7890-1234-567890abcdef
    Time: 2025-12-05T14:15:00+08:00
    Command: mv important.txt archive/
    Files backed up: 1
      - /tmp/backup-test/important.txt

[3] ID: b2c3d4e5-f6a7-8901-2345-67890abcdef1
    Time: 2025-12-05T13:00:00+08:00
    Command: rm *.log
    Files backed up: 3
      - /tmp/backup-test/app.log
      - /tmp/backup-test/error.log
      - /tmp/backup-test/debug.log

================================================================================
Total backups: 3
Storage location: ~/.aichat_backups/

Usage:
  .backup restore <id>     - Restore files from a backup
  .backup delete <id>      - Delete a specific backup
  .backup cleanup [count]  - Keep only last N backups (default: 50)
```

### 讀取備份資訊

從輸出中你可以看到：

1. **備份編號** - `[1]`, `[2]`, `[3]`
2. **備份 ID** - 唯一識別碼（用於復原）
3. **建立時間** - 備份建立的時間
4. **執行的命令** - 當時執行了什麼命令
5. **備份的檔案** - 哪些檔案被備份了

---

## 如何復原備份

### 步驟 1：找到要復原的備份 ID

```bash
ai
> .backup list

# 找到你要復原的備份 ID
# 例如：f8e961a3-b4c2-4d8f-9e7a-1234567890ab
```

### 步驟 2：執行復原命令

```bash
> .backup restore f8e961a3-b4c2-4d8f-9e7a-1234567890ab
```

### 步驟 3：確認復原成功

```
✓ Restored: /tmp/backup-test/test.txt
✓ Backup restored successfully!
✓ 1 file(s) restored
```

### 步驟 4：驗證檔案

```bash
> .exit

# 回到 shell
cd /tmp/backup-test
ls -la test.txt
cat test.txt
# 這是測試檔案 ✓
```

---

## 如何管理備份

### 1. 刪除單個備份

```bash
ai
> .backup delete a1b2c3d4-e5f6-7890-1234-567890abcdef

# 輸出：
✓ Backup deleted successfully
```

**何時刪除：**
- 確定不需要復原時
- 釋放磁碟空間
- 清理測試備份

---

### 2. 批量清理舊備份

```bash
> .backup cleanup 20

# 輸出：
✓ Cleaned up 5 old backups
✓ Kept 20 most recent backups
```

**參數說明：**
- `20` - 保留最近 20 個備份
- 省略數字 - 預設保留 50 個

**建議：**
```bash
# 每週執行一次
> .backup cleanup 50

# 如果磁碟空間緊張
> .backup cleanup 20

# 清理測試備份
> .backup cleanup 10
```

---

### 3. 檢查備份儲存位置

```bash
# 備份存放位置
ls -la ~/.aichat_backups/

# 輸出：
drwxr-xr-x  5 user  staff  160 Dec  5 15:30 .
drwxr-xr-x 50 user  staff 1600 Dec  5 15:30 ..
-rw-r--r--  1 user  staff 1234 Dec  5 15:30 backup_index.json
drwxr-xr-x  3 user  staff   96 Dec  5 15:30 f8e961a3-b4c2-4d8f-9e7a-1234567890ab/
drwxr-xr-x  3 user  staff   96 Dec  5 14:15 a1b2c3d4-e5f6-7890-1234-567890abcdef/
```

**結構：**
```
~/.aichat_backups/
├── backup_index.json              # 備份索引檔案
├── f8e961a3-.../                  # 備份 1
│   └── test.txt                   # 備份的檔案
├── a1b2c3d4-.../                  # 備份 2
│   └── important.txt
└── b2c3d4e5-.../                  # 備份 3
    ├── app.log
    ├── error.log
    └── debug.log
```

---

## 實戰範例

### 範例 1：誤刪檔案後復原

```bash
# 場景：不小心刪除了重要檔案

# 1. 建立測試檔案
cd /tmp
echo "重要的專案資料" > project.txt
cat project.txt  # 確認內容

# 2. 使用 AI 刪除（會自動備份）
ai -e "刪除 project.txt"
# 按 'e' 執行
# ✓ Backup created: abc123...

# 3. 發現刪錯了！
ls project.txt
# ls: project.txt: No such file or directory

# 4. 立即復原
ai
> .backup list
> .backup restore abc123...
> .exit

# 5. 驗證復原成功
cat project.txt
# 重要的專案資料 ✓
```

---

### 範例 2：批量刪除前預覽 + 備份

```bash
# 場景：清理 log 檔案，但想先確認

# 1. 建立多個 log 檔案
cd /tmp/logs
echo "log 1" > app.log
echo "log 2" > error.log
echo "log 3" > debug.log
echo "keep this" > important.log

# 2. 使用 AI 生成清理命令
ai -e "刪除所有以 app, error, debug 開頭的 log 檔案"

# 3. 先按 'p' 預覽
# 顯示會刪除哪些檔案：
#   - app.log ✓
#   - error.log ✓
#   - debug.log ✓
#   (important.log 不會被刪除)

# 4. 確認無誤後按 'e' 執行
# ✓ Backup created: def456...

# 5. 如果發現刪錯了
ai
> .backup restore def456...
```

---

### 範例 3：測試命令的安全模式

```bash
# 場景：不確定命令會不會誤刪，想先測試

# 1. 建立測試環境
mkdir -p /tmp/test-safe
cd /tmp/test-safe
touch file1.txt file2.txt important.txt

# 2. 測試命令
ai -e "刪除 file 開頭的檔案"

# 3. 按 'p' 預覽影響
# 顯示：
#   - file1.txt ✓
#   - file2.txt ✓
#   (important.txt 不受影響 ✓)

# 4. 按 't' 學習命令
# 顯示：rm file*.txt 的詳細解釋

# 5. 確認後按 'e' 執行
# 有備份，不怕出錯！
```

---

### 範例 4：命令執行失敗時的自動提示

```bash
# 場景：命令執行失敗

# 1. 執行一個可能失敗的命令
ai -e "移動不存在的檔案"
# 按 'e'

# 2. 執行失敗時的輸出
✓ Backup created: ghi789...
Executing: mv nonexistent.txt backup/
mv: nonexistent.txt: No such file or directory

⚠ Command failed with exit code 1
⚠ Command failed! To restore backup, run:
  .backup restore ghi789...

# 3. 如果有備份，可以立即復原
```

---

## 常見問題

### Q1: 備份會佔用很多空間嗎？

**答：** 只備份受影響的檔案，通常不會很大。

**管理方式：**
```bash
# 定期清理
ai
> .backup cleanup 30

# 查看備份大小
du -sh ~/.aichat_backups/
```

---

### Q2: 備份會自動過期嗎？

**答：** 不會自動過期，需要手動清理。

**建議：**
```bash
# 每週執行
> .backup cleanup 50
```

---

### Q3: 如果不想自動備份怎麼辦？

**答：** 選擇 `c` (copy) 複製命令後手動執行。

```bash
ai -e "刪除檔案"
# 按 'c' 複製
# 按 'q' 取消
# 手動在終端執行
```

---

### Q4: 復原會覆蓋現有檔案嗎？

**答：** 是的，復原會**覆蓋**同名檔案。

**安全做法：**
```bash
# 復原前先檢查
ls -la target_file.txt

# 如果檔案很重要，先手動備份
cp target_file.txt target_file.txt.backup

# 然後再復原
> .backup restore <id>
```

---

### Q5: 可以復原多次嗎？

**答：** 可以！同一個備份可以復原多次。

```bash
# 第一次復原
> .backup restore abc123

# 檔案被修改了

# 第二次復原（會恢復到備份時的狀態）
> .backup restore abc123
```

---

### Q6: 備份檔案的完整性如何保證？

**答：** 使用 SHA256 雜湊驗證。

**檢查方式：**
```bash
# 查看備份索引
cat ~/.aichat_backups/backup_index.json

# 會看到每個檔案的 hash
{
  "id": "abc123...",
  "files": [
    {
      "path": "/tmp/test.txt",
      "hash": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
    }
  ]
}
```

---

### Q7: 可以手動建立備份嗎？

**答：** 目前只能透過 Execute 模式自動建立。

**替代方案：**
```bash
# 使用系統的 cp 命令手動備份
cp important.txt important.txt.backup

# 或使用 rsync
rsync -av /source/ /backup/
```

---

## 📋 快速參考卡

### 基本操作

```bash
# 建立備份（自動）
ai -e "刪除檔案"
# 按 'e' 執行

# 查看備份
ai
> .backup list

# 復原備份
> .backup restore <ID>

# 刪除備份
> .backup delete <ID>

# 清理舊備份
> .backup cleanup 50
```

### 完整流程

```
1. ai -e "命令"          → 生成命令
2. 按 'p'                → 預覽影響
3. 按 't'                → 學習命令
4. 按 'e'                → 執行（自動備份）
5. .backup list          → 查看備份
6. .backup restore <ID>  → 需要時復原
```

---

## 🎯 最佳實踐

### 1. 執行前先預覽

```bash
ai -e "刪除命令"
# 總是先按 'p' 查看影響
# 確認無誤再按 'e'
```

### 2. 記下重要的備份 ID

```bash
# 執行重要操作時
ai -e "清理專案檔案"
# ✓ Backup created: abc123-def456-...

# 記下這個 ID
echo "abc123-def456-..." > ~/backup-ids.txt
```

### 3. 定期清理

```bash
# 每週執行
ai
> .backup cleanup 50
```

### 4. 重要操作前手動驗證

```bash
# 列出備份
> .backup list

# 確認最新的備份
# 然後執行操作
```

---

## 🚀 立即開始

**30 秒快速測試：**

```bash
# 1. 建立測試檔案
cd /tmp
echo "test" > test.txt

# 2. 刪除並備份
ai='/Users/morrisliao/Desktop/git-repo/aichat/aichat/target/release/aichat'
$ai -e "delete test.txt"
# 按 'e'

# 3. 復原
$ai
> .backup list
> .backup restore <顯示的ID>
> .exit

# 4. 驗證
cat test.txt  # test ✓
```

---

## 📚 總結

**備份功能的核心價值：**

1. **自動化** - Execute 模式自動建立備份
2. **安全** - 出錯也能一鍵復原
3. **簡單** - 只需要記住 `.backup` 命令
4. **可靠** - SHA256 驗證檔案完整性

**何時使用：**
- ✅ 刪除檔案前
- ✅ 移動重要檔案前
- ✅ 批量修改檔案前
- ✅ 測試新命令時

**立即開始：**
```bash
alias ai='/Users/morrisliao/Desktop/git-repo/aichat/aichat/target/release/aichat'
ai -e "你的命令"
```

祝使用愉快！🎉
